import { Block } from "payload/types";

const ContactBlock : Block = {
    slug: 'contactBlock',
    labels: {
        singular:'Contact Block',
        plural: 'Contact Block',
    },
    fields:[]
}

export default ContactBlock