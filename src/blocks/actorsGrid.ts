import { Block } from "payload/types";

const ActorsGrid: Block = {
    slug: 'actorsGrid',
    labels: {
        singular:'Actors Grid',
        plural: 'Actors Grid',
    },
    fields:[
    ]
}

export default ActorsGrid